# 腾讯云 API 凭证
TENCENT_SECRET_ID=your_secret_id
TENCENT_SECRET_KEY=your_secret_key

# 腾讯云区域
TENCENT_REGION=ap-guangzhou

# 安全组 ID
SECURITY_GROUP_ID=sg-xxxxxxxx

# 放行端口 (ALL 表示所有端口，或指定具体端口如 22,80,443)
TARGET_PORT=ALL

# 访问密钥 (建议使用 32+ 字符的强随机字符串)
# 生成命令: openssl rand -hex 32
ACCESS_TOKEN=your_strong_random_access_token_here

# 启用请求签名验证 (可选，推荐用于公网服务器)
# true:  启用签名验证 (高安全，防重放攻击，TOKEN 泄露仅 5 分钟有效)
# false: 仅验证 TOKEN (简单，适合内网或受控环境)
#
# 何时启用:
# - 服务器暴露在公网
# - 有安全合规要求 (ISO 27001, SOC 2 等)
# - TOKEN 泄露风险较高
#
# 何时禁用:
# - 仅内网使用
# - 所有客户端都在受控环境
# - 定期轮换 TOKEN
ENABLE_SIGNATURE=false
